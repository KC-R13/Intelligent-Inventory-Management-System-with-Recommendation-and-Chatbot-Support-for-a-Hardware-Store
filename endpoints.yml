# This file contains the different endpoints your bot can use.

# Server which runs your custom actions.
# Start it with: rasa run actions
# https://rasa.com/docs/rasa/custom-actions

action_endpoint:
  url: "http://localhost:5055/webhook"

# Server where models are pulled from (optional).
# https://rasa.com/docs/rasa/model-storage

#models:
#  url: http://my-server.com/models/default_core@latest
#  wait_time_between_pulls: 10

# Tracker store — stores conversations.
# Default is in-memory. Use Redis or MongoDB for production.
# https://rasa.com/docs/rasa/tracker-stores

#tracker_store:
#  type: redis
#  url: localhost
#  port: 6379
#  db: 0
#  password: <password>
#  use_ssl: false

#tracker_store:
#  type: mongod
#  url: mongodb://localhost:27017
#  db: rasa
#  username: <username>
#  password: <password>

# Event broker — stream all conversation events (optional).
# https://rasa.com/docs/rasa/event-brokers

#event_broker:
#  url: localhost
#  username: username
#  password: password
#  queue: queue